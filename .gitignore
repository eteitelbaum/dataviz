.Rproj.user
.Rhistory
.RData
.Ruserdata

/.quarto/
.Rdata
.httr-oauth
.DS_Store

# Local working notes / execution plans — not part of the published course
# (no trailing slash: these are now symlinks into ~/Documents/dataviz-notes,
# and a trailing-slash pattern doesn't match a symlink, only a real directory)
/notes

# single-file quarto render byproducts
/site_libs/
/*_files/
weeks/*_files/
assignments/*_files/
project/*_files/
modules/*_files/
slides/*_files/

# Local quiz files — not part of the published course
# (no trailing slash: see note above /notes)
/quizzes
